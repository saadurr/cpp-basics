#include <iostream>
#include <array>
//static arrays are arrays that do not grow, and are constant in size.

int main()
{
	std::array<int, 5> data;
	//sort function, size, same performance as c-style arrays

	std::cin.get();
	return 0;
}